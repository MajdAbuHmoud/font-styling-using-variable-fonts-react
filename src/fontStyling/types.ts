/*--------------------------------------------------
You will need to add these types, because we are 
gonna use them through this example
---------------------------------------------------- */

export type CustomFontFamilyType = {
  key: string;
  weightOptions: KeyValuePair[];
};

export type KeyValuePair = {
  key: string;
  value: string;
};
